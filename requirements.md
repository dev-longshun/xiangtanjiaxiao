# 湘潭驾校评价平台 - 需求文档

## 一、术语表 (Glossary)

| 术语                       | 定义                                                                            |
| -------------------------- | ------------------------------------------------------------------------------- |
| 用户 (User)                | 普通注册用户，可浏览驾校信息、提交评价、查看个人投稿状态                        |
| 管理员 (Admin)             | 拥有 `ROLE_ADMIN` 角色的用户，可审核投稿、管理驾校数据                          |
| 投稿 (Submission)          | 用户提交的评价内容，初始状态为"待审核"                                          |
| 审核状态 (Status)          | 投稿的三种状态：`PENDING`（待审核）、`APPROVED`（已通过）、`REJECTED`（已驳回） |
| 驳回原因 (Reject Reason)   | 管理员驳回投稿时填写的说明文字                                                  |
| 证明图片 (Evidence Images) | 用户投稿时上传的截图证明，最多 9 张                                             |

---

## 二、用户认证模块

### Requirement 1.1: 用户注册

**User Story**: 作为新用户，我希望通过填写昵称和密码完成注册，以便获得账号并使用投稿功能。

**Acceptance Criteria**:

1. `GIVEN` 用户访问注册页面
2. `WHEN` 用户填写昵称（2-20 字符）和密码（6-20 字符）并提交
3. `THEN` 系统应校验昵称是否已被占用
4. `WHERE` 昵称未被占用，`THEN` 系统生成唯一用户 ID（从 10001 递增），创建账号，返回登录凭证（JWT Token）
5. `WHERE` 昵称已被占用，`THEN` 提示"该昵称已被使用，请更换"

---

### Requirement 1.2: 用户登录

**User Story**: 作为已注册用户，我希望通过昵称和密码登录，以便使用个人功能。

**Acceptance Criteria**:

1. `GIVEN` 用户访问登录页面
2. `WHEN` 用户输入昵称和密码并提交
3. `THEN` 系统校验昵称和密码是否匹配
4. `WHERE` 校验通过且角色为 `ROLE_USER`，`THEN` 登录成功，跳转至首页
5. `WHERE` 校验通过且角色为 `ROLE_ADMIN`，`THEN` 登录成功，跳转至管理后台首页
6. `WHERE` 校验失败，`THEN` 提示"昵称或密码错误"

---

### Requirement 1.3: 用户登出

**User Story**: 作为已登录用户，我希望能够退出登录，以便切换账号或保护隐私。

**Acceptance Criteria**:

1. `WHEN` 用户点击"退出登录"
2. `THEN` 系统清除本地登录凭证，跳转至首页，恢复未登录状态

---

## 三、用户投稿模块

### Requirement 2.1: 在线投稿表单

**User Story**: 作为已登录用户，我希望在"投稿说明"页面直接填写表单提交评价，以便更便捷地分享学车经历。

**Acceptance Criteria**:

1. `GIVEN` 用户已登录并访问投稿页面
2. `THEN` 页面应在原有 QQ/邮箱投稿方式基础上，新增"在线投稿"表单模块
3. `AND` 表单包含以下字段：
   - 选择驾校（下拉列表，必填）
   - 评分（1-5 星，必填）
   - 评价内容（文本框，10-500 字，必填）
   - 上传证明图片（可选，最多 9 张，单张 ≤5MB，支持 jpg/png/gif/webp）
4. `WHEN` 用户填写完成并点击"提交"
5. `THEN` 系统校验表单数据，上传图片，创建投稿记录（状态为 `PENDING`）
6. `AND` 提示"投稿成功，等待管理员审核"

---

### Requirement 2.2: 未登录投稿引导

**User Story**: 作为未登录用户，我希望在尝试投稿时被引导登录，以便完成投稿流程。

**Acceptance Criteria**:

1. `GIVEN` 用户未登录
2. `WHEN` 用户访问投稿页面或点击"提交"按钮
3. `THEN` 显示提示"请先登录后再投稿"，并提供登录/注册入口

---

## 四、用户个人中心模块

### Requirement 3.1: 个人中心入口

**User Story**: 作为已登录用户，我希望能够访问个人中心，以便管理我的账号和查看投稿状态。

**Acceptance Criteria**:

1. `GIVEN` 用户已登录
2. `THEN` 页面 Header 应显示用户昵称和"个人中心"入口
3. `WHEN` 用户点击"个人中心"
4. `THEN` 跳转至个人中心页面

---

### Requirement 3.2: 我的投稿列表

**User Story**: 作为已登录用户，我希望查看我提交的所有评价及其审核状态，以便了解投稿进度。

**Acceptance Criteria**:

1. `GIVEN` 用户访问个人中心
2. `THEN` 显示"我的投稿"列表，包含以下信息：
   - 驾校名称
   - 评价内容摘要
   - 评分
   - 提交时间
   - 审核状态（待审核 / 已通过 / 已驳回）
3. `WHERE` 状态为"已驳回"，`THEN` 显示驳回原因

---

### Requirement 3.3: 修改密码

**User Story**: 作为已登录用户，我希望能够修改密码，以便保障账号安全。

**Acceptance Criteria**:

1. `GIVEN` 用户访问个人中心的"修改密码"功能
2. `WHEN` 用户输入原密码、新密码、确认新密码并提交
3. `THEN` 系统校验原密码是否正确
4. `WHERE` 原密码正确且新密码符合规则，`THEN` 更新密码，提示"密码修改成功"
5. `WHERE` 原密码错误，`THEN` 提示"原密码错误"

---

## 五、前端数据对接模块

### Requirement 4.1: 驾校列表数据对接

**User Story**: 作为用户，我希望看到的驾校信息来自数据库实时数据，以便获取最新信息。

**Acceptance Criteria**:

1. `WHEN` 用户访问首页或"所有驾校"页面
2. `THEN` 前端调用 `GET /api/schools` 获取驾校列表
3. `AND` 页面渲染数据库中的驾校数据（而非本地 JSON）

---

### Requirement 4.2: 驾校详情数据对接

**User Story**: 作为用户，我希望查看驾校详情时获取实时数据，包括最新评分和评价。

**Acceptance Criteria**:

1. `WHEN` 用户访问驾校详情页
2. `THEN` 前端调用 `GET /api/schools/{id}` 获取驾校信息
3. `AND` 调用 `GET /api/reviews/school/{schoolId}` 获取该驾校的已审核评价
4. `AND` 仅展示状态为 `APPROVED` 的评价

---

### Requirement 4.3: 评分实时更新

**User Story**: 作为用户，我希望驾校评分能反映最新的评价数据，以便做出准确判断。

**Acceptance Criteria**:

1. `WHEN` 管理员审核通过一条评价
2. `THEN` 后端自动重新计算该驾校的平均评分和评价数
3. `AND` 前端下次请求时获取到更新后的数据

---

## 六、后台管理 - 投稿审核模块

### Requirement 5.1: 待审核投稿列表

**User Story**: 作为管理员，我希望查看所有待审核的投稿，以便进行审核操作。

**Acceptance Criteria**:

1. `GIVEN` 管理员已登录并进入管理后台
2. `THEN` 显示"投稿审核"页面，列出所有状态为 `PENDING` 的投稿
3. `AND` 列表包含：驾校名称、投稿者昵称、评价摘要、评分、提交时间
4. `AND` 支持点击查看投稿详情

---

### Requirement 5.2: 投稿详情查看

**User Story**: 作为管理员，我希望查看投稿的完整内容和证明图片，以便判断真实性。

**Acceptance Criteria**:

1. `WHEN` 管理员点击某条投稿
2. `THEN` 显示投稿详情，包括：
   - 完整评价内容
   - 评分
   - 证明图片（可点击放大查看）
   - 投稿者信息
   - 提交时间

---

### Requirement 5.3: 审核通过操作

**User Story**: 作为管理员，我希望将真实有效的投稿标记为通过，以便发布到网站。

**Acceptance Criteria**:

1. `WHEN` 管理员点击"通过"按钮
2. `THEN` 系统将该投稿状态更新为 `APPROVED`
3. `AND` 自动重新计算关联驾校的评分和评价数
4. `AND` 该评价在前端驾校详情页可见

---

### Requirement 5.4: 审核驳回操作

**User Story**: 作为管理员，我希望驳回虚假或不当投稿，并告知用户原因。

**Acceptance Criteria**:

1. `WHEN` 管理员点击"驳回"按钮
2. `THEN` 弹出输入框，要求填写驳回原因（必填，10-200 字）
3. `WHEN` 管理员填写原因并确认
4. `THEN` 系统将该投稿状态更新为 `REJECTED`，保存驳回原因
5. `AND` 用户可在个人中心查看该投稿的驳回状态和原因

---

## 七、后台管理 - 驾校管理模块

### Requirement 6.1: 驾校列表

**User Story**: 作为管理员，我希望查看所有驾校信息，以便进行管理。

**Acceptance Criteria**:

1. `GIVEN` 管理员进入"驾校管理"页面
2. `THEN` 显示所有驾校列表，每条包含：名称、评分、评价数、地址
3. `AND` 支持搜索驾校名称

---

### Requirement 6.2: 驾校详情查看

**User Story**: 作为管理员，我希望查看驾校的详细信息和关联评价，以便全面了解情况。

**Acceptance Criteria**:

1. `WHEN` 管理员点击某驾校
2. `THEN` 显示驾校详情页，包含：
   - 基本信息（名称、地址、电话、营业时间、价格范围、简介）
   - 课程和标签
   - 考试数据（各科通过率）
   - 该驾校的所有评价列表（含各状态）

---

### Requirement 6.3: 新增驾校

**User Story**: 作为管理员，我希望添加新的驾校信息，以便扩充平台数据。

**Acceptance Criteria**:

1. `WHEN` 管理员点击"新增驾校"并填写表单
2. `THEN` 系统创建新驾校记录，初始评分为 0，评价数为 0
3. `AND` 新驾校在前端可见

---

### Requirement 6.4: 编辑驾校

**User Story**: 作为管理员，我希望修改驾校信息，以便保持数据准确。

**Acceptance Criteria**:

1. `WHEN` 管理员编辑驾校信息并保存
2. `THEN` 系统更新驾校记录
3. `AND` 前端展示更新后的信息

---

### Requirement 6.5: 删除驾校

**User Story**: 作为管理员，我希望删除不再运营的驾校，以便保持数据整洁。

**Acceptance Criteria**:

1. `WHEN` 管理员点击"删除"并确认
2. `THEN` 系统执行逻辑删除（`is_active = 0`）
3. `AND` 该驾校在前端不再显示
4. `AND` 关联的评价数据保留但不展示

---

### Requirement 6.6: 管理驾校评价

**User Story**: 作为管理员，我希望在驾校详情中管理该驾校的评价，以便处理问题评价。

**Acceptance Criteria**:

1. `GIVEN` 管理员查看某驾校详情
2. `THEN` 显示该驾校的所有评价（含待审核、已通过、已驳回）
3. `AND` 支持删除评价（逻辑删除）
4. `WHEN` 删除已通过的评价，`THEN` 自动重新计算驾校评分

---

## 八、技术约束与边界

### 8.1 后端调整项

1. **登录接口调整**：支持"按昵称查询用户"进行登录验证（当前是按用户名/数字 ID 查询）
2. **数据库调整**：`reviews` 表新增 `reject_reason VARCHAR(500)` 字段存储驳回原因
3. **昵称唯一性约束**：`users` 表 `nickname` 字段需添加唯一索引

### 8.2 本期不实现

1. **教练数据管理**：预留后续扩展，本期不实现

### 8.3 安全约束

1. 管理后台路由需进行权限校验，非管理员访问时跳转至登录页
2. 所有需要登录的接口必须校验 JWT Token 有效性
